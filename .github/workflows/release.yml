name: Build and Release Extension

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.18.0'
          
      - name: Install dependencies
        run: npm install
        
      - name: Build extension
        run: npm run build:all
        
      - name: Get version from tag
        id: get_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
        
      - name: Create Chrome extension zip
        run: |
          cd dist
          zip -r ../reload4u-chrome-v${{ steps.get_version.outputs.VERSION }}.zip .
          cd ..
          
      - name: Create Firefox extension zip
        run: |
          cd dist
          zip -r ../reload4u-firefox-v${{ steps.get_version.outputs.VERSION }}.zip .
          cd ..
          
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            reload4u-chrome-v${{ steps.get_version.outputs.VERSION }}.zip
            reload4u-firefox-v${{ steps.get_version.outputs.VERSION }}.zip
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}